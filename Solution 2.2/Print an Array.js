function solve(array, input){

      let Theinput = [];
      for (let index = 0; index < array.length; index++) {
          Theinput.push[array[index]];
         
          
      }
       console.log(array.join(input));
}
solve(['One', 'Two', 'Three', 'Four', 'Five'], '-');